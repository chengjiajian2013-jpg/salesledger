# SalesLedger 生产部署指南

## 🚀 部署步骤

### 1. 配置生产访问密钥

在部署前，需要先设置生产环境的访问密码（这是一个 Cloudflare Secret，不会出现在代码中）：

```bash
cd D:/cjj/workdesk/SalesLedger
npx wrangler secret put ACCESS_KEY
```

执行后会提示你输入密码，输入后按回车（密码不会显示）。

**建议密码要求：**
- 至少 12 个字符
- 包含大小写字母、数字和特殊字符
- 不要使用常见词汇

### 2. 部署到生产

```bash
npm run deploy
```

### 3. 验证部署

访问 https://salesledger.chengjiajian2013.workers.dev

- 应该看到登录页面
- 输入你设置的密码
- 登录成功后进入应用

### 4. 推送到 GitHub

```bash
git push origin main
```

## 📋 部署后检查清单

- [ ] 访问生产 URL 显示登录页面（不是主应用）
- [ ] 输入错误密码显示错误提示
- [ ] 输入正确密码成功登录
- [ ] 刷新页面保持登录状态
- [ ] 关闭标签页后重新打开需要重新登录
- [ ] 所有功能正常：添加交易、查看统计、AI 解析等

## 🔧 故障排查

### 问题：部署后无法登录（一直返回 401）

**原因：** ACCESS_KEY Secret 未配置

**解决：**
```bash
npx wrangler secret put ACCESS_KEY
# 输入你的密码
```

### 问题：登录页面没有显示，直接显示应用

**原因：** 前端代码缓存

**解决：** 
- 强制刷新页面（Ctrl+Shift+R 或 Cmd+Shift+R）
- 或清除浏览器缓存

### 问题：登录后 API 请求失败

**原因：** Token 未正确附加到请求头

**解决：**
1. 打开浏览器开发者工具 (F12)
2. 查看 Console 是否有错误
3. 查看 Network 标签，检查请求是否有 Authorization 头

## 🔐 安全提示

1. **不要**在代码、文档或 Git 中提交真实密码
2. **定期更换**生产密码（每 3-6 个月）
3. **不要分享**密码给不需要的人
4. 如果密码泄露，立即更换：
   ```bash
   npx wrangler secret put ACCESS_KEY
   # 输入新密码
   ```

## 📱 使用说明

### iPhone 添加到主屏幕

1. 在 Safari 中打开应用
2. 点击分享按钮
3. 选择"添加到主屏幕"
4. 命名为"SalesLedger"

这样就可以像原生 App 一样使用了。

### 会话管理

- **同一标签页刷新** → 保持登录
- **关闭标签页** → 退出登录
- **Token 有效期** → 12 小时
- **12 小时后** → 自动要求重新登录

## 🎯 下一步优化（可选）

1. **添加记住密码功能**：使用 localStorage 实现 30 天免登录
2. **多用户支持**：支持不同密码访问不同账本
3. **密码强度提示**：前端显示密码强度指示器
4. **登录日志**：记录登录时间和 IP（需要额外存储）

---

当前版本：v3.1 (带访问控制)
最后更新：2026-08-04
