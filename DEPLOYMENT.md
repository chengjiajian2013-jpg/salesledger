# SalesLedger 生产部署指南

## 🔐 访问密码设计

本应用采用 **iPhone 风格的 4 位数字密码**：
- 3×4 数字键盘布局
- 4 个密码点指示器
- 输入满 4 位自动验证
- 错误时红色抖动动画

## 🚀 部署步骤

### 1. 配置生产访问密码

在部署前，需要先设置生产环境的 4 位数字密码（这是一个 Cloudflare Secret，不会出现在代码中）：

```bash
cd D:/cjj/workdesk/SalesLedger
npx wrangler secret put ACCESS_KEY
```

执行后会提示你输入密码，**输入 4 位数字**（如 6789），然后按回车。

**密码要求：**
- 必须是 4 位数字
- 避免使用简单序列（如 1234, 0000, 1111）
- 建议使用不规则组合（如 7392, 5847）

### 2. 部署到生产

```bash
npm run deploy
```

### 3. 验证部署

访问 https://salesledger.chengjiajian2013.workers.dev

- 应该看到数字键盘登录界面
- 输入你设置的 4 位数字密码
- 登录成功后进入应用

### 4. 推送到 GitHub

```bash
git push origin main
```

## 📋 部署后检查清单

- [ ] 访问生产 URL 显示数字键盘登录页
- [ ] 输入错误密码显示红色抖动和错误提示
- [ ] 输入正确的 4 位数字密码成功登录
- [ ] 刷新页面保持登录状态
- [ ] 关闭标签页后重新打开需要重新输入密码
- [ ] 所有功能正常：添加交易、查看统计、AI 解析等

## 🔧 故障排查

### 问题：部署后无法登录（一直返回 401）

**原因：** ACCESS_KEY Secret 未配置

**解决：**
```bash
npx wrangler secret put ACCESS_KEY
# 输入你的 4 位数字密码
```

### 问题：数字键盘没有显示，直接显示应用

**原因：** 前端代码缓存

**解决：** 
- 强制刷新页面（Ctrl+Shift+R 或 Cmd+Shift+R）
- 或清除浏览器缓存

### 问题：登录后 API 请求失败

**原因：** Token 未正确附加到请求头

**解决：**
1. 打开浏览器开发者工具 (F12)
2. 查看 Console 是否有错误
3. 查看 Network 标签，检查请求是否有 Authorization 头

### 问题：点击数字键没有反应

**原因：** JavaScript 加载失败

**解决：**
1. 打开浏览器控制台查看错误
2. 检查网络请求是否成功加载所有 JS 模块
3. 强制刷新页面

## 🔐 安全提示

1. **不要**在代码、文档或 Git 中提交真实密码
2. **定期更换**生产密码（每 3-6 个月）
3. **不要分享**密码给不需要的人
4. 如果密码泄露，立即更换：
   ```bash
   npx wrangler secret put ACCESS_KEY
   # 输入新的 4 位数字密码
   ```

## 📱 使用说明

### iPhone 添加到主屏幕

1. 在 Safari 中打开应用
2. 点击分享按钮
3. 选择"添加到主屏幕"
4. 命名为"SalesLedger"

这样就可以像原生 App 一样使用了，体验类似 iPhone 解锁。

### 会话管理

- **同一标签页刷新** → 保持登录
- **关闭标签页** → 退出登录
- **Token 有效期** → 12 小时
- **12 小时后** → 自动要求重新输入密码

### 输入技巧

- **点击数字键**：触摸或鼠标点击
- **物理键盘**：也可以直接按键盘数字键（0-9）
- **删除**：点击 ⌫ 按钮或按键盘退格键
- **自动提交**：输入满 4 位自动验证，无需按确认

## 🎯 下一步优化（可选）

1. **生物识别**：支持 Face ID / Touch ID（需要 WebAuthn API）
2. **记住设备**：使用 localStorage 实现 30 天免登录
3. **多用户支持**：支持不同密码访问不同账本
4. **密码重试限制**：连续错误 5 次后锁定 1 分钟

---

当前版本：v3.2 (iPhone 风格数字键盘)
最后更新：2026-08-04
